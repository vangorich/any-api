# 环境配置示例
# 复制此文件为 .env 并填写实际值

# 应用配置
VITE_API_STR="/api"
VERSION="1.0.0"

# 安全配置
# ⚠️ 重要: 在生产环境中必须修改为安全的随机密钥!
SECRET_KEY="your-super-secret-key-change-it-in-production"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=43200  # 30天

# 数据库配置
# SQLite (默认)
DATABASE_URL="sqlite+aiosqlite:///./data/sql_app.db"

# 自动迁移 (默认启用)
# 设置为 true 时,应用启动时会自动执行数据库迁移
# 设置为 false 时,需要手动执行: python migrate.py upgrade
AUTO_MIGRATE=true

# 或使用 PostgreSQL
# DATABASE_URL="postgresql+asyncpg://user:pass@localhost/dbname"

# 或使用 MySQL
# DATABASE_URL="mysql+aiomysql://user:pass@localhost/dbname"

# Gemini API 配置
GEMINI_BASE_URL="https://generativelanguage.googleapis.com"

# 服务器配置 (可选)
# HOST="0.0.0.0"
# PORT=8000

# CORS 配置 (可选)
# BACKEND_CORS_ORIGINS='["http://localhost:5173", "http://localhost:3000"]'

# 初始化说明
# 首次部署后,请访问 Web 界面完成系统初始化
# 您将在初始化流程中创建管理员账户
